[workspace]
members = [
    "core",
    "cons",
    "cadr",
]
resolver = "2"

[workspace.package]
version = "0.2.0"
edition = "2024"
authors = ["Claude Code"]
license = "MIT OR Apache-2.0"

[workspace.dependencies]
core = { path = "core" }
cons = { path = "cons" }
